# 寵物美容 SaaS CRM 預約互動系統 - 產品需求文件 (PRD)

## 📋 專案概述

### 產品定位

**輕量化 CRM 預約互動系統**，專為寵物美容業者設計的 SaaS 平台，主打**即時回覆增加互動**以及**輕量管理**。

### 核心價值主張

- 🚀 **即時互動**：透過 LINE 整合，實現即時預約通知與服務回報
- 📱 **輕量管理**：簡潔直觀的管理介面，降低學習成本
- 🎯 **多租戶架構**：支援多個商家獨立運營，資料完全隔離
- 💬 **無縫溝通**：客戶無需下載額外 App，透過 LINE 即可完成所有操作

### 目標用戶

- **主要用戶**：寵物美容店老闆、店長、美容師
- **次要用戶**：寵物主人（透過 LINE 使用）

---

## 🏗️ 系統架構

### 技術棧

- **前端框架**：React 19 + TypeScript + Vite
- **樣式系統**：Tailwind CSS + SCSS（Mobile-First）
- **後端服務**：Firebase (Firestore, Functions, Storage, Hosting)
- **身份驗證**：
  - 客戶端：LINE LIFF (LINE In-App Browser)
  - 管理端：Firebase Authentication
- **即時通訊**：LINE Messaging API
- **部署平台**：Firebase Hosting + Cloud Functions

### 多租戶架構

- 每個商家擁有獨立的：
  - LINE LIFF ID
  - LINE Channel Access Token
  - Firestore 資料集合（`shops/{shopId}/...`）
  - 商家設定（服務項目、營業時間、寵物種類等）

---

## 👥 用戶角色與權限

### 1. 客戶（Customer）

- **登入方式**：LINE LIFF（透過 LINE 官方帳號）
- **權限**：
  - ✅ 查看個人資料
  - ✅ 編輯個人資料（姓名、手機）
  - ✅ 建立預約
  - ✅ 查看服務歷史
  - ✅ 取消自己的預約
  - ❌ 無法查看其他客戶資料
  - ❌ 無法管理預約狀態

### 2. 商家管理員（Admin）

- **登入方式**：Firebase Email/Password 認證
- **權限**：
  - ✅ 管理所屬商家的所有預約
  - ✅ 查看客戶列表
  - ✅ 管理服務紀錄
  - ✅ 設定商家資訊（服務項目、營業時間、LINE 設定）
  - ✅ 現場預約（Walk-in Booking）
  - ✅ 查看 LINE 訊息配額
  - ❌ 無法管理其他商家
  - ❌ 無法修改訂閱方案

### 3. 超級管理員（SuperAdmin）

- **登入方式**：Firebase Email/Password 認證（特殊權限）
- **權限**：
  - ✅ 管理所有商家
  - ✅ 建立/刪除商家帳號
  - ✅ 管理商家訂閱方案
  - ✅ 查看所有商家資料
  - ✅ 不受訂閱限制影響

---

## 🎯 核心功能模組

## 一、客戶端功能（LINE LIFF）

### 1.1 首頁（Home）

**路徑**：`/`

**功能描述**：

- 顯示客戶個人資訊（姓名、頭像、手機）
- 顯示商家 Logo（如有設定）
- 編輯個人資料功能
- 顯示服務歷史記錄（最近完成的服務）
- 固定底部「我要預約」按鈕

**主要功能**：

- ✅ LINE 自動登入（透過 LIFF）
- ✅ 個人資料編輯（姓名、手機號碼驗證）
- ✅ 服務歷史列表（顯示最近完成的預約）
- ✅ 快速進入預約流程

**UI/UX 特點**：

- 響應式設計（Mobile-First）
- 簡潔直觀的介面
- 支援圖片懶加載

---

### 1.2 預約流程（Booking）

**路徑**：`/booking`

**功能描述**：
多步驟預約流程，引導客戶完成預約

**步驟一：選擇服務**

- 顯示商家提供的服務項目
- 顯示服務價格、時長
- 選擇日期（日曆選擇器）
- 選擇時間（根據營業時間和已預約時段動態顯示）

**步驟二：填寫寵物資訊**

- 寵物名稱（必填）
- 寵物種類（狗、貓、兔子等）
- 寵物體型（小型、中型、大型）
- 寵物品種（選填）
- 寵物照片（選填，支援相機拍攝）
- 備註（選填）

**步驟三：確認預約**

- 顯示預約摘要
- 確認後提交

**主要功能**：

- ✅ 即時檢查可用時段（避免重複預約）
- ✅ 時間衝突檢測（Transaction 確保資料一致性）
- ✅ 圖片上傳與壓縮（客戶端壓縮後上傳）
- ✅ 預約成功後自動發送 LINE 訊息通知
- ✅ 預約成功後可選擇關閉視窗

**技術亮點**：

- Firestore Transaction 確保時段不重複
- 圖片客戶端壓縮（使用 browser-image-compression）
- 支援相機直接拍攝（`capture="environment"`）

---

### 1.3 服務歷史（Service History）

**路徑**：首頁內嵌顯示

**功能描述**：

- 顯示客戶所有已完成的預約
- 顯示服務日期、服務項目、寵物資訊
- 顯示服務紀錄（臨時回報、完成分享）

**顯示內容**：

- 預約日期與時間
- 服務項目名稱
- 寵物名稱與照片
- 服務狀態（已完成）
- 服務紀錄（如有）

---

## 二、管理端功能（Admin Dashboard）

### 2.1 管理員登入（Admin Login）

**路徑**：`/admin/login`

**功能描述**：

- Firebase Email/Password 認證
- 訂閱狀態檢查（停用/過期帳號無法登入）
- 顯示聯繫客服資訊（如訂閱異常）

**主要功能**：

- ✅ 登入驗證
- ✅ 訂閱狀態檢查
- ✅ 自動跳轉到 Dashboard
- ✅ 錯誤提示

---

### 2.2 管理儀表板（Admin Dashboard）

**路徑**：`/admin/dashboard`

**功能描述**：
商家管理後台主頁，提供多個功能模組切換

**主要標籤頁**：

1. **預約列表**（Appointments）
2. **日曆視圖**（Calendar）
3. **客戶管理**（Customers）
4. **服務紀錄**（Service Records）
5. **商家設定**（Settings）

**即時通知功能**：

- ✅ 新預約即時通知（Toast）
- ✅ 預約取消通知
- ✅ 預約狀態變更通知
- ✅ 使用 Firestore `onSnapshot` 實現即時更新

**搜尋與篩選**：

- ✅ 客戶名稱搜尋
- ✅ 月份篩選
- ✅ 服務項目篩選
- ✅ 狀態篩選（待確認、已確認、已完成、已取消）

---

### 2.3 預約管理

#### 2.3.1 預約列表（Appointment List）

**功能描述**：

- 顯示所有預約（可篩選）
- 預約卡片顯示：
  - 客戶姓名、手機
  - 寵物資訊（名稱、種類、體型、照片）
  - 服務項目、價格、時長
  - 預約日期與時間
  - 預約狀態
  - 備註
- 點擊預約卡片可查看詳情

**操作功能**：

- ✅ 確認預約（pending → confirmed）
- ✅ 完成預約（confirmed → completed）
- ✅ 取消預約（任何狀態 → cancelled）
- ✅ 查看預約詳情（彈窗顯示完整資訊）

**狀態流程**：

```
pending（待確認） → confirmed（已確認） → completed（已完成）
                ↘ cancelled（已取消）
```

---

#### 2.3.2 日曆視圖（Calendar View）

**功能描述**：

- 月曆顯示所有預約
- 點擊日期查看當日預約列表
- 視覺化顯示預約分佈

**主要功能**：

- ✅ 月曆導航（上一個月/下一個月）
- ✅ 日期點擊查看詳情
- ✅ 預約數量標記
- ✅ 響應式設計

---

#### 2.3.3 行動裝置日檢視（Mobile Daily View）

**路徑**：`/admin/mobile`

**功能描述**：
專為行動裝置優化的當日預約檢視

**主要功能**：

- ✅ 顯示今天的預約（按時間排序）
- ✅ 即時時間軸顯示
- ✅ 預約狀態優先級排序（pending > confirmed > completed）
- ✅ 點擊預約查看詳情
- ✅ 快速操作（確認、完成、取消）
- ✅ 自動滾動到當前時間

**UI 特點**：

- 時間軸視覺化
- 當前時間標記
- 觸控優化

---

### 2.4 客戶管理（Customer List）

**功能描述**：

- 顯示所有客戶列表
- 客戶資訊：
  - 姓名、手機
  - LINE 頭像
  - 預約次數
  - 最後預約日期
- 點擊客戶查看詳細預約歷史

**主要功能**：

- ✅ 客戶搜尋（姓名、手機）
- ✅ 客戶詳情查看
- ✅ 預約歷史列表
- ✅ 客戶統計資訊

---

### 2.5 服務紀錄管理（Service Records）

**功能描述**：
管理已完成預約的服務紀錄，包括臨時回報和完成分享

**主要功能**：

- ✅ 顯示所有已完成的預約
- ✅ 篩選功能（月份、服務項目）
- ✅ 查看服務紀錄：
  - 臨時回報（圖片 + 訊息）
  - 完成分享（圖片 + 訊息）
  - 顧客注記
- ✅ 發送服務紀錄到 LINE：
  - 發送臨時回報
  - 發送完成分享
- ✅ 紀錄發送歷史

**服務紀錄類型**：

1. **臨時回報**（Temp Report）
   - 服務進行中的照片與訊息
   - 可多次發送
2. **完成分享**（Completion）
   - 服務完成的照片與訊息
   - 通常只發送一次

**技術實現**：

- 圖片上傳到 Firebase Storage
- 透過 Cloud Function 發送 LINE 訊息
- 紀錄發送時間與內容

---

### 2.6 現場預約（Walk-in Booking）

**路徑**：`/admin/walk-in-booking`

**功能描述**：
商家可直接為現場客戶建立預約，無需客戶透過 LINE

**主要功能**：

- ✅ 選擇服務項目
- ✅ 選擇日期與時間
- ✅ 填寫客戶資訊（姓名、手機）
- ✅ 填寫寵物資訊（名稱、種類、體型、照片）
- ✅ 建立預約（標記為 walk-in）
- ✅ 不發送 LINE 通知（walk-in 預約）

**使用場景**：

- 客戶直接到店預約
- 電話預約
- 補登預約記錄

---

### 2.7 商家設定（Shop Settings）

**功能描述**：
管理商家的所有設定資訊

#### 2.7.1 基本資訊

- 商家名稱
- 商家 Logo（上傳圖片）

#### 2.7.2 服務項目管理

- 新增/編輯/刪除服務項目
- 設定服務名稱、價格、時長
- 支援多個服務項目

#### 2.7.3 營業時間設定

- 設定每日營業時間（開始時間、結束時間）
- 選擇營業日（週一至週日）
- 支援不同日期不同營業時間

#### 2.7.4 寵物種類與體型設定

- 設定支援的寵物種類（狗、貓、兔子等）
- 設定支援的寵物體型（小型、中型、大型）

#### 2.7.5 LINE 整合設定

- LIFF ID（LINE In-App Browser ID）
- Channel ID（LINE Messaging API Channel ID）
- Channel Access Token（長期 Token）
- LINE 訊息配額顯示與刷新

#### 2.7.6 訂閱狀態顯示

- 顯示訂閱方案（試用期/月訂閱/年訂閱）
- 顯示訂閱狀態（啟用中/已停用/已過期）
- 顯示到期日期與剩餘天數
- 剩餘天數 ≤ 7 天時顯示警告

---

## 三、超級管理員功能（SuperAdmin）

### 3.1 商家管理（Shop Manager）

**路徑**：`/superadmin`

**功能描述**：
管理所有商家帳號與訂閱

**主要功能**：

- ✅ 商家列表顯示
- ✅ 建立新商家
- ✅ 編輯商家資訊
- ✅ 刪除商家
- ✅ 訂閱管理：
  - 設定訂閱方案（試用期/月訂閱/年訂閱）
  - 設定訂閱狀態（啟用/停用）
  - 續訂功能
  - 修改方案
- ✅ 篩選功能：
  - 即將到期（7 天內）
  - 按訂閱狀態篩選
  - 按訂閱方案篩選

**訂閱方案**：
| 方案 | 識別碼 | 期限 | 說明 |
|------|--------|------|------|
| 試用期 | `trial` | 3 個月 | 新商家體驗 |
| 月訂閱 | `monthly` | 1 個月 | 短期使用 |
| 年訂閱 | `yearly` | 12 個月 | 長期客戶 |

**訂閱狀態**：
| 狀態 | 識別碼 | 說明 |
|------|--------|------|
| 啟用中 | `active` | 正常使用中 |
| 已停用 | `inactive` | 手動停用，無法登入 |
| 已過期 | `expired` | 自動過期，無法登入 |

---

## 四、LINE 訊息通知系統

### 4.1 預約確認通知

**觸發時機**：客戶建立預約後

**發送內容**：

- Flex Message 格式
- 顯示預約詳情：
  - 寵物名稱
  - 服務項目
  - 預約日期
  - 預約時間
- 包含「查看預約詳情」按鈕（連結到 LIFF）

**技術實現**：

- Cloud Function: `onAppointmentCreated`
- 監聽 `shops/{shopId}/appointments/{appointmentId}` 文件創建事件
- 自動發送 LINE 訊息

---

### 4.2 預約狀態變更通知

**觸發時機**：管理員確認或取消預約時

**發送內容**：

- 預約已確認通知
- 預約已取消通知

**技術實現**：

- Cloud Function: `onAppointmentStatusConfirmed`
- 監聽預約狀態更新事件

---

### 4.3 服務完成通知

**觸發時機**：管理員發送完成分享時

**發送內容**：

- 服務完成照片
- 服務完成訊息
- 感謝訊息

**技術實現**：

- Cloud Function: `sendLineCompletionMessage`
- HTTP 觸發（由前端呼叫）

---

### 4.4 臨時回報通知

**觸發時機**：管理員發送臨時回報時

**發送內容**：

- 服務進行中照片
- 服務進行中訊息

**技術實現**：

- Cloud Function: `sendLineTempReportMessage`
- HTTP 觸發（由前端呼叫）

---

### 4.5 LINE 訊息配額管理

**功能描述**：

- 顯示當前 LINE Messaging API 配額使用情況
- 配額刷新功能
- 配額不足提醒

**技術實現**：

- Cloud Function: `getLineMessageQuota`
- 查詢 LINE Messaging API 配額 API

---

## 五、資料模型

### 5.1 用戶（User）

```typescript
interface User {
  uid: string; // LINE User ID
  displayName: string;
  pictureUrl?: string;
  phone?: string;
  createdAt: Timestamp;
  role: "customer";
}
```

**儲存位置**：`users/{userId}`

---

### 5.2 商家（Shop）

```typescript
interface Shop {
  id: string;
  name: string;
  logoUrl?: string;
  services: Service[];
  businessHours: BusinessHours;
  petSpecies?: string[];
  petSizes?: string[];

  // LINE 設定
  liffId: string;
  lineChannelId: string;
  lineChannelAccessToken: string;

  // 訂閱資訊
  subscription?: Subscription;
}

interface Service {
  id?: string;
  name: string;
  duration: number; // 分鐘
  price: number;
}

interface BusinessHours {
  start: string; // "HH:mm"
  end: string; // "HH:mm"
  daysOpen: number[]; // 0=Sunday, 1=Monday, ...
}

interface Subscription {
  plan: "monthly" | "yearly" | "trial";
  status: "active" | "inactive" | "expired";
  startDate: string; // ISO string
  expiryDate: string; // ISO string
  autoRenew: boolean;
  createdAt: string;
  updatedAt: string;
}
```

**儲存位置**：`shops/{shopId}`

---

### 5.3 預約（Appointment）

```typescript
interface Appointment {
  id: string;
  shopId: string;
  userId: string; // LINE User ID 或 "walk-in-{timestamp}"
  customerName: string;
  phone?: string;
  petName?: string;
  petSpecies?: string;
  petSize?: string;
  petPhoto?: string;
  serviceType: string;
  servicePrice?: number;
  duration: number; // 分鐘
  date: string; // YYYY-MM-DD
  time: string; // HH:mm
  notes?: string;
  createdAt: Timestamp;
  status: "pending" | "confirmed" | "cancelled" | "completed";
}
```

**儲存位置**：`shops/{shopId}/appointments/{appointmentId}`

---

### 5.4 每日時段（Daily Schedule）

```typescript
interface DailySchedule {
  date: string; // YYYY-MM-DD
  bookings: {
    start: number; // 分鐘（從 00:00 開始）
    end: number; // 分鐘
    appointmentId: string;
  }[];
}
```

**儲存位置**：`shops/{shopId}/daily_schedules/{date}`

**用途**：

- 快速檢查可用時段
- 避免時間衝突
- 使用 Transaction 確保一致性

---

### 5.5 服務紀錄（Service Report）

```typescript
interface ServiceReport {
  id: string;
  appointmentId: string;
  shopId: string;
  customerId: string;
  customerName: string;
  petName?: string;

  // 臨時回報
  tempReportImage?: string;
  tempReportMessage?: string;
  tempReportTimestamp?: Timestamp;

  // 完成分享
  completionImage?: string;
  completionMessage?: string;
  completionTimestamp?: Timestamp;

  // 設定
  serviceNotes?: string;
  isCompleted: boolean;
  completedAt?: Timestamp;

  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

**儲存位置**：`shops/{shopId}/serviceReports/{reportId}`

---

### 5.6 寵物（Pet）

```typescript
interface Pet {
  id: string;
  name: string;
  species: string;
  size: string;
  breed?: string;
  photoUrl?: string;
  notes?: string;
  createdAt?: Timestamp;
}
```

**儲存位置**：`users/{userId}/pets/{petId}`

---

### 5.7 管理員（Admin）

```typescript
interface Admin {
  uid: string;
  email: string;
  shopId: string; // 所屬商家 ID
  role: "admin" | "superadmin";
  createdAt: Timestamp;
}
```

**儲存位置**：`admins/{adminId}`

---

## 六、安全性與權限控制

### 6.1 Firestore Security Rules

- **客戶端**：LINE LIFF 無 Firebase Auth，使用 Cloud Function 驗證
- **管理端**：Firebase Auth 驗證，檢查 `admins` 集合權限
- **多租戶隔離**：每個商家資料完全隔離（`shops/{shopId}/...`）
- **預約建立**：任何人都可以建立（客戶端預約）
- **預約更新**：客戶只能取消，管理員可以更新所有狀態
- **商家設定**：只有該商家管理員或 SuperAdmin 可以更新

### 6.2 Storage Security Rules

- 圖片上傳需要驗證
- 每個商家只能存取自己的圖片

---

## 七、效能優化

### 7.1 前端優化

- ✅ Code Splitting（React.lazy + Suspense）
- ✅ 圖片懶加載（LazyImage 組件）
- ✅ 客戶端圖片壓縮（上傳前壓縮）
- ✅ 響應式設計（Mobile-First）
- ✅ 即時更新（Firestore onSnapshot）

### 7.2 後端優化

- ✅ Firestore Indexes 優化
- ✅ Transaction 確保資料一致性
- ✅ Cloud Functions 冷啟動優化
- ✅ 圖片壓縮與快取

---

## 八、部署與維護

### 8.1 部署流程

1. **前端部署**：Firebase Hosting
2. **後端部署**：Cloud Functions
3. **資料庫設定**：Firestore Rules + Indexes
4. **儲存設定**：Storage Rules

### 8.2 環境變數

- `VITE_LIFF_ID`：LINE LIFF ID（開發環境）
- `VITE_FIREBASE_PROJECT_ID`：Firebase 專案 ID
- Firebase Functions 環境變數（生產環境）

### 8.3 監控與日誌

- Cloud Functions 日誌
- Firestore 使用量監控
- LINE Messaging API 配額監控

---

## 九、未來規劃功能

### 9.1 短期規劃（P1）

- [ ] 預約提醒通知（預約前一天提醒）
- [ ] 客戶評價系統
- [ ] 預約統計報表
- [ ] 多語言支援（繁體中文、英文）

### 9.2 中期規劃（P2）

- [ ] 會員積分系統
- [ ] 優惠券功能
- [ ] 預約候補功能
- [ ] 商家多分店支援

### 9.3 長期規劃（P3）

- [ ] AI 客服機器人
- [ ] 預約推薦系統
- [ ] 商家聯盟功能
- [ ] 行動 App（iOS/Android）

---

## 十、技術債務與已知問題

### 10.1 技術債務

- [ ] LINE LIFF 無 Firebase Auth，需加強 Cloud Function 驗證
- [ ] 圖片儲存優化（CDN 整合）
- [ ] 錯誤處理統一化
- [ ] 單元測試覆蓋率提升

### 10.2 已知限制

- LINE Messaging API 配額限制
- Firestore 讀寫配額限制
- Cloud Functions 冷啟動延遲

---

## 十一、成功指標（KPI）

### 11.1 業務指標

- 商家註冊數量
- 活躍商家數量（月活躍）
- 訂閱轉換率（試用 → 付費）
- 客戶留存率

### 11.2 產品指標

- 預約建立成功率
- LINE 訊息送達率
- 系統響應時間
- 錯誤率

### 11.3 用戶滿意度

- 商家滿意度調查
- 客戶滿意度調查
- NPS（Net Promoter Score）

---

## 附錄

### A. 專案結構

```
pet-medical-crm/
├── src/
│   ├── components/          # 共用組件
│   ├── contexts/            # React Context（認證、狀態管理）
│   ├── features/            # 功能模組
│   │   ├── admin/           # 管理端功能
│   │   ├── appointments/    # 預約相關
│   │   ├── pets/            # 寵物相關
│   │   └── superadmin/      # 超級管理員功能
│   ├── hooks/               # Custom Hooks
│   ├── lib/                 # 工具庫（Firebase、LIFF）
│   ├── types/               # TypeScript 類型定義
│   └── styles/              # SCSS 樣式
├── functions/               # Cloud Functions
│   └── src/
│       └── index.ts         # 所有 Cloud Functions
├── public/                  # 靜態資源
└── Documents/               # 文件資料夾
```

### B. 主要路由

```
客戶端（LINE LIFF）：
- /                    → 首頁
- /booking             → 預約流程

管理端（Firebase Auth）：
- /admin/login         → 管理員登入
- /admin/dashboard     → 管理儀表板
- /admin/mobile        → 行動裝置日檢視
- /admin/walk-in-booking → 現場預約

超級管理員：
- /superadmin          → 商家管理
```

### C. Cloud Functions 列表

1. `onAppointmentCreated` - 預約建立通知
2. `onAppointmentStatusConfirmed` - 預約狀態變更通知
3. `getLineMessageQuota` - 取得 LINE 訊息配額
4. `sendServiceCompletionNotification` - 發送服務完成通知
5. `sendLineCompletionMessage` - 發送完成訊息
6. `sendLineTempReportMessage` - 發送臨時回報訊息
7. `sendTestMessages` - 測試訊息發送
8. `declineAppointment` - 拒絕預約
9. `checkExpiredSubscriptions` - 檢查過期訂閱（定時任務）
10. `manualCheckExpiredSubscriptions` - 手動檢查過期訂閱

---

## 版本歷史

| 版本   | 日期    | 說明                   |
| ------ | ------- | ---------------------- |
| v1.0.0 | 2024-12 | 初始版本，包含核心功能 |
| -      | -       | 訂閱制功能實作         |
| -      | -       | 多租戶架構升級         |

---

**文件最後更新**：2024-12-17  
**維護者**：開發團隊  
**聯絡方式**：support@petcrm.com
